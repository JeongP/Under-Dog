<%= form_for(biding) do |f| %>
  <% if biding.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(biding.errors.count, "error") %> prohibited this biding from being saved:</h2>

      <ul>
      <% biding.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :price %>
    <%= f.number_field :price %>
  </div>

  <div class="field">
    <%= f.label :success_bid %>
    <%= f.check_box :success_bid %>
  </div>

  <div class="field">
    <%= f.label :post_id %>
    <%= f.number_field :post_id %>
  </div>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.number_field :user_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
